name: 'Setup Android Environment'
description: 'Checks out code and sets up Java, Android SDK, and Gradle'
runs:
  using: 'composite'
  steps:
    - name: Checkout main repository
      uses: actions/checkout@v4

    - name: Clean older folder to download new one
      run: rm -rf ./about-module
      shell: bash

    - name: Checkout about-module
      uses: actions/checkout@v4
      with:
        repository: ${{ github.actor }}/japl-android-about-module
        path: ./about-module

    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Set up Android SDK
      uses: android-actions/setup-android@v3

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
